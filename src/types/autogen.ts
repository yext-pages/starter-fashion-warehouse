export default interface Product {
  slug: string;
}

export interface ImageType {
  url: string;
  alt?: string;
}

export interface YextImage {
  url: string;
  alt?: string;
  width: number;
  height: number;
}
